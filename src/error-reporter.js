function reportError(error, context = {}) {
    // A centralized error reporting function.
    // Replace with a Sentry call or equivalent when available.
    console.error("REPORTED ERROR:", error, context);
}
